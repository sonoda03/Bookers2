<h1>User info</h1>
<p><%= image_tag user.get_profile_image(100,100) %></p>
<tbody>
  <tr></tr>
  <tr>
    <th>name</th>
    <th><%= user.name %></th>
  </tr>
  <tr>
    <th>introduction</th>
    <th><%= user.name %></th>
  </tr>
</tbody>


<h1>New book</h1>

<%= form_with model: book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  
  <P><%= f.submit "Create Book" %></P>
<% end %>
